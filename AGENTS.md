# Repository instructions

Keep `skills/flat/SKILL.md` in step with the CLI. Any change to commands,
ticket fields, conflict handling, or other behavior described by the skill
must update the skill in the same change.

Do not account for backward compatibility anywhere in the app.
