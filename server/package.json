{
  "name": "@flat/server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "test": "vitest run",
    "typecheck": "tsc --noEmit && tsc --noEmit -p test"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260823.1",
    "@types/node": "^24",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.48.0"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
