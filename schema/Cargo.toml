[package]
name = "flat-schema"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "flat tee wire protocol: Rust structs are the source of truth; TS types are generated"

[dependencies]
serde.workspace = true
serde_json.workspace = true
schemars = "0.8"

[[bin]]
name = "codegen"
path = "src/bin/codegen.rs"
