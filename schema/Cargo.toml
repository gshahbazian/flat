[package]
name = "flat-schema"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[lib]
name = "flat_schema"

[dependencies]
serde = { version = "1", features = ["derive"] }
typeshare = "1"

[dev-dependencies]
serde_json = "1"
